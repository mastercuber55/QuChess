{
  "type": "module",
  "scripts": {
    "build": "npm i && cd web && npm i && npm run build && cd ..",
    "serve": "node index.js"
  },
  "dependencies": {
    "express": "^5.2.1",
    "socket.io": "^4.8.3"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.3.0",
    "globals": "^17.6.0"
  }
}
